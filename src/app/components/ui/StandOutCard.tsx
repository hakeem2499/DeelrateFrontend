import React from 'react'

type Props = {
    title:React.ReactNode;
    description:React.ReactNode;
    icon:React.ReactNode;
    index:number;
}

const StandOutCard = ({title,description,icon, index}: Props) => {
  return (
    <div>StandOutCard</div>
  )
}

export default StandOutCard